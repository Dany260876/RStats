const rsConfig = {
    initialize : () => {
        // Load page
        rsConfig.initContent();
    },
    initContent: () => {
        console.log('rsConfig loaded');
    },
}

export default rsConfig;